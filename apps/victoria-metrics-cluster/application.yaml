apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: victoria-metrics-cluster
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: https://github.com/VictoriaMetrics/helm-charts.git
      targetRevision: victoria-metrics-cluster-0.35.0
      path: charts/victoria-metrics-cluster
      helm:
        valueFiles:
          - $values/apps/victoria-metrics-cluster/values.yaml
    - repoURL: git@github.com:muzzai/monitoring_backends.git
      targetRevision: HEAD
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
